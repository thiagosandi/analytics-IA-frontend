import { Routes } from '@angular/router';
import { DashboardCentral } from './dashboard-central';

export const DASHBOARD_CENTRAL_ROUTES: Routes = [
    {
        path: '',
        component: DashboardCentral,
    },
]